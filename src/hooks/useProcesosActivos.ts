// ARCHIVO A BORRAR — `rm src/hooks/useProcesosActivos.ts`
//
// El contenido se movió a `useProcesosFallidos.ts`: el archivo exportaba un solo hook
// y el nombre no coincidía con lo que hacía.
//
// Se deja vacío en vez de borrado sólo para que el repo compile antes del `rm`.

export {};
